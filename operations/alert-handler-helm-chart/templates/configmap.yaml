apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "alert-handler.fullname" . }}
  labels:
    {{- include "alert-handler.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "alert-handler.config" . | nindent 4 }}
