apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-handler.fullname" . }}
  labels:
    {{- include "alert-handler.labels" . | nindent 4 }}
  {{- with .Values.podAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alert-handler.selectorLabels" . | nindent 4 }}
